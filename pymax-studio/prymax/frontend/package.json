{
  "name": "prymax-studio-frontend-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Real-browser tests for the PryMax Studio room client, using Playwright + Node's built-in test runner. Requires the Flask backend running on localhost:5000 (see ../../backend/README instructions).",
  "scripts": {
    "test": "node --test tests/"
  },
  "devDependencies": {
    "playwright": "^1.56.0"
  }
}
